WINDOW
variables:
	GLFWwindow* window
	int width
	int height
methods:
	bool isActive();
	
UTILS
methods:
	vector<float> computeNormals(vector<float> posBuf, vector<unsigned int> indBuf);
	float rand();
